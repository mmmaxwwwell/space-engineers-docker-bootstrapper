{
  "name": "app",
  "dependencies": {
    "aws-sdk": "^2.797.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "moment": "^2.28.0",
    "moment-timezone": "^0.5.31",
    "node-persist": "^3.1.0",
    "nodemon": "^2.0.4"
  },
  "scripts": {
    "dev": "nodemon -x node -r dotenv/config main.js dotenv_config_path=../secrets/.env",
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^26.6.3"
  }
}
